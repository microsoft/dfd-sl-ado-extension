steps:
- task: DefenderForDevOpsCodeToCloud@0
  displayName: '(Task) Microsoft Defender for DevOps Container Mapping Start'
  timeoutInMinutes: 1
  condition: always()
  continueOnError: true
  inputs:
    tasktype: 'pre-job' 